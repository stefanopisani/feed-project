<div class="page-top"> </div>
<div class="container">
  <div class="row">
    <div class="col"> </div>
    <div class="col-6">
      <section class="card-template">
          <h4> Favorites </h4>
          <hr>
        <section class="favorites-info">
            {{#each favorites}}
            <h4> {{recipe.title}} </h4>

<section class="user-profile-recipe">
  <article class="user-profile-text">
    <h6> Description </h6>
    <p> {{recipe.description}}</p>
  </article>
  <article class="user-profile-post-img-parent">
    <img src="{{recipe.imageUrl}}" class=".favorites-food-img user-profile-post-img img-fluid" alt="Responsive image">
  </article>
</section>
        <section class="favorites-btns">
        <form action="/recipe-details/{{recipe.id}}" method="GET">
          <button class="btn btn-outline-secondary" type="submit">Recipe Details</button>
        </form>
        <form action="/delete-favorite/{{id}}" method="POST">
          <button class="btn btn-outline-secondary" type="submit"> Delete </button>
        </form>
      </section>
                    <hr>
            {{/each}}
          </section>
    </section>
</div>
<div class="col"></div>
</div>
</div>